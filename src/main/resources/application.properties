spring.application.name=webhook

# We drain request bodies ourselves off the InputStream, so never let Spring's
# multipart resolver buffer uploads to memory/disk.
spring.servlet.multipart.enabled=false
